name: media_repo_local
description: Local-FS / OPFS-backed implementation of MediaRepository.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  cross_file: ^0.3.4
  drift: ^2.21.0
  drift_flutter: ^0.2.0
  meta: ^1.12.0
  path: ^1.9.0
  path_provider: ^2.1.5

  core_api:
    path: ../core_api
  media_repo_api:
    path: ../media_repo_api

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  drift_dev: ^2.21.0
