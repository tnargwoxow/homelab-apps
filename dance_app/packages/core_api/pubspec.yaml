name: core_api
description: Shared value types and errors for dance_app. Pure Dart.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  meta: ^1.12.0

dev_dependencies:
  test: ^1.25.0
