apiVersion: v1
kind: Secret
metadata:
  name: masterclass-credentials
  namespace: default
  labels:
    app: masterclass-downloader
type: Opaque
stringData:
  # Base64 encode your credentials before adding them here
  # To encode: echo -n "your-email@example.com" | base64
  # Or use stringData (as shown) which Kubernetes will auto-encode
  MASTERCLASS_USERNAME: "Bienchen550@aol.com"
  MASTERCLASS_PASSWORD: "subfaz-duPce2-fuxsas"

