name: video_engine_api
description: Interface for the dance_app video playback engine. Pure Dart.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  core_api:
    path: ../core_api
  meta: ^1.12.0

dev_dependencies:
  test: ^1.25.0
